// Copia este archivo como config.js para probar en local. En AWS lo genera Terraform (infra/web.tf)
// con los valores reales, así que no se versiona.
window.APP_CONFIG = {
  apiUrl: "https://xxxxxxxxxx.execute-api.us-east-1.amazonaws.com",
  clientId: "xxxxxxxxxxxxxxxxxxxxxxxxxx",
  region: "us-east-1",
};
