## Resumen
<!-- Qué cambia y por qué. Enlaza el UC/TC o spec afectado. -->

## Spec y documentación
- [ ] El documento AIUP correspondiente (`Status` del UC/TC, entity model, requisitos) está actualizado en este mismo PR.
- [ ] Los aprendizajes o dolores quedaron en `docs/charla/`.

## Pruebas
- [ ] `uv run ruff check . && uv run pytest`
- [ ] `terraform fmt -check` y `validate` (si toca `infra/`)
